{
  "name": "ruby-website",
  "version": "1.0.0",
  "description": "Site vitrine Ruby + journal (blog) — généré avec Eleventy.",
  "private": true,
  "scripts": {
    "start": "eleventy --serve",
    "build": "eleventy"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0"
  }
}
